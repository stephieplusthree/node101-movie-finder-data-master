import React from 'react';

function Footer() {
    return (
        <h1 style={style.container}>Footer</h1>
    );
}

const style = ({
    container: {
        padding: "100px",
        color: "white",
        background: "black",
        marginTop: "60px",
    }
})
export default Footer;